{
  "name": "crm-vega",
  "version": "1.0.0",
  "description": "CRM system with Telegram bot",
  "scripts": {
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3"
  }
}

